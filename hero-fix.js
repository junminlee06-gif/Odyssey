(() => {
  // PNG hero rendering is handled by sprite-cropper.js and game.js.
  // This file intentionally does not override drawHero.
  // It remains loaded only as a safe compatibility placeholder.
})();
